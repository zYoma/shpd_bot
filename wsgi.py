from config import app
import main
# from config import IP

# if __name__ == "__main__":
#     ip_port = IP.split(':')
#     app.run(host=ip_port[0], port=ip_port[1], use_reloader=True, debug=True,
#             ssl_context=('webhook_cert.pem', 'webhook_pkey.pem'))
